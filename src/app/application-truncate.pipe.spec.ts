import {ApplicationTruncatePipe} from './application-truncate.pipe';

describe('ApplicationTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
